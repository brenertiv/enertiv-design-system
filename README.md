# Enertiv design system catalog

MUI v6 catalog for designers and PMs. Production remains the source of truth.

```bash
npm install
npm run dev
```

Open `http://localhost:5173`. Sections: Tokens, Primitives, Icons, Composites, Heavy patterns.
