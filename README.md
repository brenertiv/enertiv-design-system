# Enertiv design system catalog

MUI v6 catalog for designers and PMs. Production remains the source of truth.

```bash
npm install
npm run dev
```

Open `http://localhost:5173`. Start at **System → Overview**. Remaining nav matches production `pages/Sandbox`.

Agent-only rules: [`docs/agents/enertiv-design-system.md`](docs/agents/enertiv-design-system.md).
